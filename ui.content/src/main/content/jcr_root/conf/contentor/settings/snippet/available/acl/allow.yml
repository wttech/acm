name: acl/allow
content: |
  acl.allow { authorizable = "${1:authorizable}"; path = "${2:path}"; glob="/*"; permissions = [${3:permissions}]; restrictions = [:]; skipPathMissing() }
documentation: |
    Allow the given permissions on the given path for the given authorizable.
