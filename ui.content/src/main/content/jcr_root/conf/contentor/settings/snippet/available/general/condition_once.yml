group: General
name: condition_once
content: condition.once()
documentation: |
  Execute the script only once per its path.
  When the script content is changed, the script will not be executed again.
  Alias for `condition.oncePerExecutableId()`.
