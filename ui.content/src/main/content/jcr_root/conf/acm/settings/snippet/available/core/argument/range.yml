group: Argument
name: argument_range
content: |
  args.range("${1:name}") { label = "${2:label}"; value = ${3:value}; min = ${4:min}; max = ${5:max}; step = ${6:step} }
documentation: |
  An argument that allows to select a range number with min and max values.
