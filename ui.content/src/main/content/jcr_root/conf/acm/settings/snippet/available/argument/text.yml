group: Argument
name: argument_text
content: |
  args.text("${1:name}") { label = "${2:label}"; value = "${3:value}"; language = "${4:language}" }
documentation: |
  An argument holding a complex, multiline string value.<br>
  Language can be specified for syntax highlighting, for example, "json" or "xml".<br>
