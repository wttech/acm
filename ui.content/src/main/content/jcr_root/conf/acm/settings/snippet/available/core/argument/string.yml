group: Argument
name: argument_string
content: |
  args.string("${1:name}") { label = "${2:Label}"; value = "${3:value}" }
documentation: |
  An argument holding a string value.<br>
  
  For example:
  ```groovy
  args.string("foo") { label = "Foo"; value = "bar" }
  ```
