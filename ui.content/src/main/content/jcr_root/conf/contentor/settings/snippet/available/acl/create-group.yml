name: acl/create-group
content: |
  group = acl.createGroup { id = "${1:id}" }
documentation: |
  Create or retrieve the group with the given ID.
  The group will only be created if it does not already exist.
