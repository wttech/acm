group: Repo
name: repo_delete
content: |
  repo.get("${1:path}").delete()
documentation: |
  Delete a resource from the repository.
  
  For example:
  ```groovy
  repo.get("/content/acme").delete()
  ```