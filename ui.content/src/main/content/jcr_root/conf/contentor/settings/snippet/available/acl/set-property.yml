name: acl/set-property
content: |
  user = acl.setProperty { authorizable = ${1:authorizable}; relPath = "${2:relPath}"; value = "${3:value}" }
documentation: |
  Set the given property with given repPath and value to the given authorizable.
