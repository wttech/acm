group: Condition
name: condition_passed
content: |
  conditions.passed(${1:duration})
documentation: |
  Wait for the specified duration before allowing the script to execute again.
  
  For example:
  ```groovy
  conditions.passed(Duration.ofDays(1))
  conditions.passed(3600) // seconds (1 hour)
  ```