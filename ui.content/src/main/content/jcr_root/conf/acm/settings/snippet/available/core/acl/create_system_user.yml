group: ACL
name: acl_create_system_user
content: |
  acl.createUser { id = "${1:id}"; systemUser() }
documentation: |
  Create or retrieve the system user with the given ID.<br>
  The user will only be created if it does not already exist.
