group: Condition
name: condition_none_running
content: |
  conditions.noneRunning()
documentation: |
  Execute the script only if none of the scripts are running.
  
  Ensures that the script is not executed in parallel (including other scripts).
  This condition guarantees that on cluster nodes only one instance of any script is running at a time.
