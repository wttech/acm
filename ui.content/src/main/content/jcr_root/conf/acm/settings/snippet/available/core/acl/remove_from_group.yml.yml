group: ACL
name: acl_remove_from_group
content: |
  acl.removeFromGroup { groupId = "${1:groupId}"; authorizableId = "${2:authorizableId}" }
documentation: |
  Removes the given authorizable (user or group) from the given group.
