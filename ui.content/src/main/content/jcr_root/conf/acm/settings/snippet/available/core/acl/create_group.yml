group: ACL
name: acl_create_group
content: |
  acl.createGroup { id = "${1:id}" }
documentation: |
  Create or retrieve the group with the given ID.<br>
  The group will only be created if it does not already exist.
