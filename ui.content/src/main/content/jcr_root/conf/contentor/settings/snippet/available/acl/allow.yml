group: ACL
name: acl_allow
content: |
  acl.allow { authorizable = ${1:authorizable}; path = "${2:path}"; permissions = [${3:permissions}]; glob = "/*"; restrictions = [:] }
documentation: |
  Allow the specified permissions, glob and restrictions on the given path for the target authorizable.
