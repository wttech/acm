name: acl/deny
content: |
  acl.deny { authorizable = ${1:authorizable}; path = "${2:path}"; permissions = [${3:permissions}]; glob="/*"; restrictions = [:] }
documentation: |
  Deny the given permissions on the given path for the given authorizable.
