name: acl/create-user
content: |
  def ${1:user} = acl.createUser { id = "${2:id}"; fullName = "${3:fullName}"; password = "${4:password}"; email = "${5:email}" }
  acl.setPassword { authorizable = ${1:user}; password = "${2:password}" }
  acl.setProperty { authorizable = ${1:user}; relPath = "${2:relPath}"; value = "${3:value}" }
  acl.removeProperty { authorizable = ${1:user}; relPath = "${1:relPath}" }
  acl.removeFromAllGroups { authorizable = ${1:user} }
  acl.clear { authorizable = ${1:user}; path = "${2:path}" }
  acl.purge { authorizable = ${1:user} }
  acl.addToGroup { authorizable = ${1:user}; group = ${2:group} }
  acl.removeFromGroup { authorizable = ${1:user}; group = ${2:parent} }
  acl.allow { authorizable = ${1:user}; path = "${2:path}"; permissions = [${3:permissions}]; glob = "/*"; restrictions = [:] }
  acl.deny { authorizable = ${1:user}; path = "${2:path}"; permissions = [${3:permissions}]; glob = "/*"; restrictions = [:] }
documentation: |
  Create or retrieve the user with the given ID, full name, password and email.
  The user will only be created if it does not already exist.
  Then further commands are performed for this user.
