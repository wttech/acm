group: Condition
name: condition_instance_run_mode
content: |
  conditions.isInstanceRunMode('${1:runMode}')
documentation: |
  Execute the script only if instance is in the specified run mode.<br>
  For example `conditions.isInstanceRunMode('prod')`.