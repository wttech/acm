package dev.vml.es.acm.core.code.schedule;

import dev.vml.es.acm.core.code.Schedule;

public class BootSchedule implements Schedule {

    @Override
    public String getId() {
        return "boot";
    }
}
