group: Condition
name: condition_passed_minutes
content: |
  condition.passedMinutes(${1:minutes})
documentation: |
  Execute the script only after a certain number of minutes have passed since the last execution.<br>
  The script content is not considered.
  
  For example:
  ```groovy
  condition.passedMinutes(10)
  ```