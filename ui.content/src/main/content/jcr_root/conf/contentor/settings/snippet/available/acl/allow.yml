name: acl/allow
content: |
  acl.allow { authorizable = ${1:authorizable}; path = "${2:path}"; permissions = [${3:permissions}]; glob="/*"; restrictions = [:] }
documentation: |
  Allow the given permissions on the given path for the given authorizable.
