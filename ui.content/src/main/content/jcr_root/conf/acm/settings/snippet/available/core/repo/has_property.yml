group: Repo
name: repo_has_property
content: |
  repo.get("${1:path}").hasProperty("${2:key}")
documentation: |
  Check if the resource in the repository has the given property.
  
  For example:
  ```groovy
  repo.get("/content/acme/us/en/jcr:content").hasProperty("foo")
  ```