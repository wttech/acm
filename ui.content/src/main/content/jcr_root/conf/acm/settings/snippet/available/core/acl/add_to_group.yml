group: ACL
name: acl_add_to_group
content: |
  acl.addToGroup { groupId = "${1:groupId}"; authorizableId = "${2:authorizableId}" }
documentation: |
  Add the given authorizable (user or group) to the target group.
